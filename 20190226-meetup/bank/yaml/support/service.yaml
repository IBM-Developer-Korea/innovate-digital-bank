apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  name: "innovate-support"
spec:
  type: NodePort
  ports:
  - port: 4000
    nodePort: 30180
  selector:
    app: "innovate-support-selector"
