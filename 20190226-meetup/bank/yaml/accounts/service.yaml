apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  name: "innovate-accounts"
spec:
  type: NodePort
  ports:
  - port: 3400
    nodePort: 30120
  selector:
    app: "innovate-accounts-selector"
