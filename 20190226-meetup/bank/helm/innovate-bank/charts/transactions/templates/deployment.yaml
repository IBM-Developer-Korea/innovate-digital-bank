apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ template "transactions.fullname" . }}
  labels:
    app: {{ template "transactions.name" . }}
    chart: {{ template "transactions.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: "{{ template "transactions.name" . }}-selector"
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: "{{ template "transactions.name" . }}-selector"
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3000
            periodSeconds: 1000
          env:
            - name: PORT
              value : "{{ .Values.service.port }}"
            - name: MONGO_URL
              valueFrom:
                configMapKeyRef:
                  name: "{{ .Release.Name }}-innovate-bank-configmap"
                  key: mongoUrl
            - name: BASE_PATH
              valueFrom:
                configMapKeyRef:
                  name: "{{ .Release.Name }}-innovate-bank-configmap"
                  key: basePath
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
