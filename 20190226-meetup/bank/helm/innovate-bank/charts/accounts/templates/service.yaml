apiVersion: v1
kind: Service
metadata:
  name: {{ template "accounts.fullname" . }}
  labels:
    app: {{ template "accounts.name" . }}
    chart: {{ template "accounts.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: "{{ template "accounts.name" . }}-selector"
    release: {{ .Release.Name }}
