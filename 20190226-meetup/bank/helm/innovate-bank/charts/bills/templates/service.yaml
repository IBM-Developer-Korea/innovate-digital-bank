apiVersion: v1
kind: Service
metadata:
  name: {{ template "bills.fullname" . }}
  labels:
    app: {{ template "bills.name" . }}
    chart: {{ template "bills.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: "{{ template "bills.name" . }}-selector"
    release: {{ .Release.Name }}
